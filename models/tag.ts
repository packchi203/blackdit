export interface TagModel {
     id: number
     icon:string
     slug:string
     color_bg:string
     name: string
     desciption: string
     tag_follow_count: number
     follow: boolean,
     posts_use:number,
     bg_color:string,
     important:boolean,
     createdAt:string
}